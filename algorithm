>>>快慢指针法：
用来判断单链表中是否存在环，具体做法是设置两个指针，初始时都指向链表的头结点，快指针每次移动两步，慢指针每次移动一步，(可以证明)如果
存在环，那么着两个指针一定相遇。
那么如何寻找环的入口？
我们设链表头结点距离环的入口为x,环入口距离两个指针相遇节点距离为z，环的长度为l,那么两个指针相遇时满足X2 = 2*X1,其中X1 = x + z;
X2 = x + nl + z;合并两式有 x + z = n*l;于是可以通过这样的方式找到环的入口：让一个指针从连表头出发，另一个指针从相遇点出发，步长均为1
，那么他们相遇的节点就是环的入口。(可以这样理解，由于 x+z = n*l，那么步长相同的两个指针一定会在z处相遇，又它们的最后一段路程是重合的
，会共同走过最后一段路，所以他们肯定会在环的入口相遇)
